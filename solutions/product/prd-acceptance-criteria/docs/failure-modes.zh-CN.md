# PRD 与验收标准 · 失败模式

- `UNTESTABLE_CRITERIA`：验收标准无法验证或依赖主观判断。
- `INVENTED_COMMITMENT`：把未确认的决策写成确定交付。
- `MISSING_NON_GOAL`：范围没有显式非目标，后续蔓延。
- `INSTRUCTION_INJECTION`：模型执行了工作流/约束输入内嵌的指令。
- `MISSING_FIELD`：输出合同字段缺失。

