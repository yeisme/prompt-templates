# Q 版像素战斗精灵图（4×4 透明 RGBA，人工审阅译文）

> 本文档是人工审阅译文，不进入编译。编译与投递只使用 `prompts/main.en.md`。

【核心目标】输出一张真正透明背景的 RGBA PNG 精灵图，必须：精确 4 列 × 4 行共 16 个顺序帧；从左到右、从上到下；所有单元格尺寸完全一致；固定相机；统一角色比例；稳定地面基线；统一注册点/锚点；每格只出现一个完整角色；角色、武器和特效不超出单元格；四周保留足够透明留白；不含文字、数字、编号、网格线、水印、边框或背景场景。

【角色保真度】严格参考上传角色图像，转换为可识别的 Q 版像素艺术精灵，准确保留：发型与发色、脸部主要特征、服装结构、主配色关系、武器或装备、身体比例、独特装饰。全部 16 帧保持角色身份、头身比、发型结构、服装结构、武器尺寸造型、主辅色关系、像素比例一致。禁止变脸、换装、武器变形、颜色漂移或比例变化。

【战斗动作】优先沿用角色原始战斗逻辑；无明确设定时：近战武器以武器攻击为核心，远程武器设计射击/投射，元素/魔法/能量用匹配属性特效，都没有则设计自然近战物理攻击。

【16 帧动作流程】准备→预备→蓄力→起手→攻击→冲击峰值→跟随→特效消散→恢复→回准备。每帧必须有真实逐帧变化：身体姿势、重心、脚步支撑、躯干扭转、手臂发力、腿部动作、武器轨迹、衣物头发惯性、特效出现增强消散。禁止平移/旋转/缩放/复制静态角色伪造动画。第 16 帧必须自然接回第 1 帧。推荐分配：1–3 准备与预备；4–5 蓄力起手；6–8 高速攻击；9 冲击峰值；10–11 跟随与消散；12–15 恢复；16 接近第 1 帧。

【像素风格】高质量 16 位游戏风：像素边缘锐利、无模糊插值、轮廓清楚、体块明确、剪影易读、调色板克制统一、明暗层级清晰、统一像素尺度、专业游戏 Sprite 完成度。避免现代高分辨率插画质感、柔和笔刷、大量半透明抗锯齿、模糊边缘、非像素化特效。

【透明背景】真正透明 RGBA：纯净透明、无背景色、无棋盘格、无黑白纯色底、无低透明彩色残留、无半透明脏边、无噪点。角色之外干净透明，边缘清晰像素化裁切。发光特效控制在合理范围，不污染透明区域。

【最终检查】16 帧、4×4、顺序、单元格一致、单角色、无裁切、尺寸稳定、基线稳定、注册点一致、身份服装武器一致、真实逐帧、节奏清楚、可循环、真透明、无文字网格水印脏像素。最终只生成这一张 4×4 透明 PNG 精灵图。
