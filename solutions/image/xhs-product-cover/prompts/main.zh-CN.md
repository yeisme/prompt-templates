# 小红书产品封面视觉提示

> 本文档是人工审阅译文，不进入编译。编译与投递只使用对应 `.en.md`。

为产品 `{{product}}` 设计一张小红书封面图的生成提示。目标受众是 `{{audience}}`，核心卖点是 `{{selling_point}}`，视觉风格为 `{{visual_style}}`。

请输出：

1. 主体、构图、景别、光线、色彩和材质描述。
2. 标题文案的安全排版区域；不要要求模型生成难以控制的长文字。
3. 画面比例 `{{aspect_ratio}}` 和留白要求。
4. 必须避免的品牌错误、乱码、畸形手部、重复物体和误导性效果。
5. 一条主提示、一条保守修复提示和负面约束。

不要指定不存在的产品功能，也不要伪造认证、效果对比或用户评价。
