# Profile：Stylized Animation

`profile_id`：`stylized_animation`。maturity：`exploratory`；readiness：`candidate`；rights：只使用 Owner 明示投影的 style、角色、资产和声音权利；不得把参考风格、角色或 IP 的相似性视为已授权。

共享状态语义保持为：source projection → active bindings → semantic proposal → Owner candidate/review；不接受资产、不产生 canonical revision/digest、不发起渲染。

风险优先级：shape language、turnaround、形变边界、色彩/材质一致、动作节奏和多视图身份可读。证据最低要求：style rule 投影、face/body/wardrobe/turnaround binding 摘要、形变允许/禁止项、场景配色/光源规则、时长窗。

建议时长：将单个动作节拍保持在 2–6 秒；涉及夸张形变、变身或多角色追踪时拆成 setup/contact/result。最终镜头长度、帧率和交付规格由 Owner 决定。readiness 只可建议 prototype/candidate，不能因单帧美观而升格。

特有 QC：表情表仍是脸部级资产、服装不能改写身份锚点、形变不越过明确限制、正背/侧视仍同一角色、色彩和材质状态跨镜连续。
