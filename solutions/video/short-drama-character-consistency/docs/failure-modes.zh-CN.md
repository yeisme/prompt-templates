# 短剧人物一致性 · 失败模式

- `CHARACTER_DRIFT`：脸型、服装或标志物与设定不一致。
- `SILENT_CONFLICT_RESOLUTION`：冲突被静默消解或重写角色设定。
- `UNDECLARED_ELEMENT`：新增未声明的角色、道具或伤痕。
- `INSTRUCTION_INJECTION`：模型执行了连续性输入内嵌的指令。
- `MISSING_FIELD`：输出合同字段缺失。

