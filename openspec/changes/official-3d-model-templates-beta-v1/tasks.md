# 实施与验收任务

由 scripts/openspec-tasks.py 维护状态。

- [x] 1 内容 owner：用 Registry CLI 为 `3d` package 生成 5 个 beta solution 骨架（`text-to-3d-object-beta`、`image-to-3d-refine-beta`、`3d-scene-layout-beta`、`3d-printable-design-beta`、`3d-asset-review-beta`）并设置 tags/capabilities；依赖无；验证 `catalog build` 与 `catalog validate` 通过；失败重检 solution id、package 目录与 metadata 重放；单线程执行。 | evidence: 2026-09-11 `solution add`（en 模板+tags/capabilities）与 `solution locale describe`（zh-CN 元数据）×5 全部经 `scripts/author_3d_templates.py` CLI 序列完成；catalog build/validate 连续两次同 digest `sha256:9de636245d2b5943f3ec07633e3201dae5c41573268ccc9de6e7638627ac8932`（46→51 solutions）
- [x] 2 内容 owner：撰写 `text-to-3d-object-beta` 与 `3d-printable-design-beta` 英文模板正文、contract、评审清单与失败模式；依赖 1；验证 contract 校验与编译演练（缺字段拒绝、成功导出、provider_calls=0）；失败重检变量与必填字段。 | evidence: `contract validate` success ×2；`scripts/check_3d_templates.py` 通过——空会话/未确认/缺必填（relations、asset_ref）/非法枚举（purpose=sculpture、support_strategy=maybe）编译均拒绝，成功导出+bundle verify+无未解析占位符，provider_calls=0
- [x] 3 内容 owner：撰写 `image-to-3d-refine-beta` 与 `3d-scene-layout-beta` 英文模板正文与 contract，场景字段保持 provider-neutral；依赖 1；验证 contract 校验与编译演练；失败重检变量与来源声明；可与任务 2 并行。 | evidence: `contract validate` success ×2；参考图 regex 拒绝非 promptrepo ref；模板正文与 zh-CN 译文均显式声明 provider-neutral 空间字段、不耦合 SceneGEO；编译演练同上全绿（provider_calls=0）
- [x] 4 内容 owner：撰写 `3d-asset-review-beta` 评审清单模板（几何完整性、UV/贴图、比例、rights_review）；依赖 1；验证 contract 校验；可与任务 2、3 并行。 | evidence: `contract validate` success；四节清单（几何完整性/UV 贴图/比例一致性/rights_review）逐条 pass/fail/unknown 三态，检查脚本编译演练全绿
- [x] 5 内容 owner：用 Registry CLI 注册 recipe `concept-to-3d-asset-beta`，步骤引用 exact `locale=en` ref；依赖 2、3、4；验证 recipe DAG、依赖循环与失效传播校验；失败重检步骤 ref 与输出绑定。 | evidence: 经 `prompt recipe init/step/bind/preset` 生成 `recipes/concept-to-3d-asset-beta.json`；`recipe validate` 通过（拓扑序 image-to-3d→asset-review）；两步 exact `locale=en` ref 均在 catalog 解析；环 DAG 负例被拒；Eikona 概念图以外部 source 绑定进入（未确认即门控），评审步骤 `asset_ref ← step_output` 保持 needs_step_output 不编造中间产物，「显式导出」以末步 output_requirement 表达（仓内无概念图生成模板，不伪造跨仓 ref）；recipe 会话 pin 两模板成功、未确认编译拒绝
- [x] 6 内容 owner：为 5 个 solution 撰写 `docs/template-zh-CN.md` 中文审阅译文并标注人工审阅身份；依赖 2、3、4；验证变量 parity 与 locale 政策（中文不可编译、不进 catalog digest）；失败重检变量集合差异。 | evidence: 5 份译文头部均标注「人工审阅译文（待人工校对）：仅供人类阅读，不注册为可编译模板」；en 提示词/zh 译文/contract 输入三方变量集合逐一相等；catalog 模板列表仅 en；`locale=zh-CN` 会话创建被拒（5/5）
- [x] 7 文档 owner：随 solution 落地校正 `docs/adding-3d-prompts.md`、`docs/taxonomy.md` 与 `docs/README.md` 索引（本次设计阶段已写入 beta 版，实施时对齐实际 CLI 命令与链接）；依赖 1–6；验证链接与类别清单一致；失败重检索引条目。 | evidence: adding-3d-prompts.md 食谱 B/C 对齐实际 CLI 序列与 recipe 实况（含验证节）；taxonomy.md 十三类含 3d 行与注册 tags 一致（category/modality:3d、artifact:model_3d+scene_layout、constraint 三值、capability model3d+scene_spatial）；README 索引修正为十三类；docs 相对链接检查通过（根仓 check-doc-links 仅余 apigateway/aigora 两条预存 dangling，与本仓无关）
- [ ] 8 外部阶段：真实 3D provider 调用、费用授权、Scaena 字段映射与转正评审；依赖后续运行与发布授权；未完成不得转正式支持。
