# 实现与验证

## 已批准范围

通过 authoring CLI 重放 111 个语言文档的用途标注，保留 52 个方案、77 个角色与原内容哈希。

## 决策

全部启用来源参与默认列表；按方案展开角色和语言文档。默认缓存五分钟，四路并发，每来源三十秒；失败保留旧快照。媒体用途与目录分类分开，consumer 缺少声明时显示 unknown。元数据不含正文，也不授予执行权限。

## 兼容与回滚

旧 TemplateRole/Solution 位置参数构造、CLI 命令、JSON 字段、精确引用、内容哈希和用户状态路径保持兼容。发现标注是独立集合，不参与旧内容 hash。回滚使用旧 CLI/SDK；它会忽略新增发现标注，不需要删除或反向迁移用户数据。旧客户端重写快照可能丢弃标注，新列表后续刷新恢复。

## 验证证据

- temp/integration-test-runs/integration-20260914T032938.601485Z/summary.json

证据由现有测试入口生成；此命令只验证记录成功，语义验收由根线程对照用户计划完成。远端发布、部署和真实凭据配置未执行。
