# 设计

- 晋升而非新 major：用户决策（2026-09-05）——旧 1.x 已废弃，"正式版本"从 1.0.0 起版，无后缀目录是唯一官方位。
- 命名规则：非正式版本目录/版本号带 `beta`/`alpha`；转正时去后缀并重置为 1.0.0（内容整体晋升，不要求语义化连续）。
- 退役 spec 的承接：metadata-by-CLI 规则已由 `official-prompt-release` 覆盖；meta-prompt 禁令移入 `official-modular-character-asset-templates`。
