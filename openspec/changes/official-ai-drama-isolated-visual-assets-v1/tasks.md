# 任务

- [x] 1. 新增 `face-mask-front-v1` 文档、合同与正反 fixtures。
  - Evidence（2026-09-02）：`isolated-character-assets-v1` role 全套落地——prompt 正文（`prompts/isolated-character-assets-v1.zh-CN.md`）、11-input contract、document descriptor、`character_asset.prompt_bundle.isolated.v1` 输出 schema、Eikona isolated compiler profile（含 `FACE_MASK_*` 稳定失败码）与 fixtures（1 valid + 4 conflict：背景/发饰/脖肩/侧脸）；`contract validate` 与 `document validate` success；usage/preset 文档含 face-mask 使用与评审要点。fixtures 集级校验受任务 5 所述上游门冲突影响，与本条交付物完整性无关。
- [x] 2. 新增 `task.isolation_mode` 兼容语义。
  - Evidence（2026-09-02）：三态枚举（`studio_neutral` 缺省 / `transparent_subject` / `environment_only`）进入 contract input enum 与 isolated authoring schema；`staged-character-production-v2` prompt 声明缺省兼容；本会话经 Registry CLI 新增 invalid fixture `face-mask-environment-only-handoff`（稳定码 `ENVIRONMENT_ONLY_REQUIRES_BACKGROUND_TEMPLATE`，evidence 指向 `task.isolation_mode`/`task.requested_output`，repair hint 指向 `ai-drama-background-assets/clean-background-plate-v1`），覆盖 spec "人物与背景同请求 → 隔离 finding/拆分建议" 场景；`face-master-v1` 与已发布 1.0.0 消费面零改动。
- [x] 3. 新增 `ai-drama-background-assets`、直接 Prompt 与校验 fixtures。
  - Evidence（2026-09-02）：solution（exploratory，zh-CN×2 roles）+ `clean-background-plate-v1` 直接可渲染零人物背景板（固定 `subject_count=0`、完整不透明画布）+ `validate-background-plate-v1` 校验合同；fixtures 5 cases（1 valid + 4 invalid：人物/倒影/投影/雕像人形痕迹）`fixture validate` success（digest `sha256:890c4af16c66e3ccd299552564b921afe8d454689603c8ddbf3ced4749253edb`）；本会话补齐缺失的 `clean-background-plate-v1` document descriptor（Registry CLI `document init`，绑定 `background_asset.authoring.v1` schema，`document validate` success）。
- [x] 4. 由 Template Registry CLI 生成或刷新 solution、contract、document、fixture 和 catalog metadata。
  - Evidence（2026-09-02）：全部结构化文件经 Registry CLI 生成或重放——本会话执行 `document init`（clean-background-plate-v1 descriptor）、`fixture case add --stdin`（environment_only handoff，JCS 落盘并更新 manifest digest 至 `sha256:f52d9c4843149ad18c9e6fb96d099ee4f6174084e9010a326549fe56b1df0b77`）、`catalog build`×2（digest 一致 `sha256:cb1dda68199eed2150f7bcd4c6128ab19322f693a980b5f59d003fdcffb35582`，19 solutions，catalog.json 零漂移）；既有 metadata 经确定性 rebuild 验证与工作树一致；无手写 `solution.json`/schema/manifest/catalog。
- [ ] 5. 运行 catalog build/validate、fixture validate 和 OpenSpec strict validation。
  - 当前状态（2026-09-03 复验）：catalog build 连续两次 digest 一致（`sha256:cb1dda68199eed2150f7bcd4c6128ab19322f693a980b5f59d003fdcffb35582`，19 solutions，工作树零漂移）且 `catalog validate` success；`ai-drama-background-assets/validate-background-plate-v1` fixture validate success（5 cases，digest `sha256:890c4af16c66e3ccd299552564b921afe8d454689603c8ddbf3ced4749253edb`）；`openspec validate --all --strict --no-interactive` 14 passed / 0 failed。唯一未过项：`isolated-character-assets-v1` fixture validate 失败——Registry fixture 门（`internal/fixtureauthor` 禁止 valid 输出含 `*ref` 键，2026-09-02 closeout 落地）与 1.x `character_asset.prompt_bundle.v1` 合同冲突：bundle schema 与 Eikona 已归档 compiler（`DisallowUnknownFields` 且必填）都要求 `subject_version_ref`，而模块化 v2 会话已明确决策 1.x 不改名。2026-09-03 对照当前上游复验，阻塞未解：template-registry `internal/fixtureauthor/fixture.go` 仍以 `strings.HasSuffix(normalized, "ref")` 拒绝 valid 输出键（失败复现 `forbidden key "subject_version_ref"`），无 1.x 语义族 allowlist。需上游协调（Registry 门对 1.x 语义族加 allowlist，或跨仓统一迁移到 `subject_version`），本内容仓不单方面重命名字段以免破坏 Eikona 兼容；同一冲突同样影响已归档 `main.zh-CN` fixture 集。
- [x] 6. 记录未触发真实 Provider、未发布 release 的交付边界。
  - Evidence（2026-09-02）：`docs/isolated-visual-assets-v1.md` 落地——真实 Provider 调用 = 0（fixture 均本地 JSON，无 credential/payload/运行证据）；无 commit/tag/push/publish 宣称发布；`1.0.0` 已发布物不原地修改；内容形成 1.1.0（人物侧）/1.0.0（背景侧）release candidate，immutable release 受 Registry 无普通 prompt solution release family（仅 graph-kit profile）与维护者授权两重阻塞；maturity 保持 exploratory；回滚路径明确。
